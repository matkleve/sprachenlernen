# Backlog artefacts

Historical product inventories and roadmap reasoning. **Not studies, not specs.**

The live queue is [`IMPLEMENTATION-PLAN.md`](../IMPLEMENTATION-PLAN.md). Open
questions that block a specific item live as `⚠ SPEC GAP` in that use case or
spec — not here.

Format: `BL-NNN-slug.md` with `<!-- id: BL-NNN -->` and `<!-- type: backlog -->`.
