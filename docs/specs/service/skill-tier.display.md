# Skill tier — display rules

<!-- parent: SPEC-service-skill-tier -->
<!-- status: active -->

Wood visible when &lt;3 qualify; dropped at ≥3. Rank highest tier first; cap
three + `+` overflow. See parent spec.

## Check

`npm test -- skill-tier method-detail-badge-band`
