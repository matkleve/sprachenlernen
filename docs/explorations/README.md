# Explorations

Owner and designer brainstorms. **Not binding** until graduated into a use case
and spec.

Format: `EXP-NNN-slug.md` with `<!-- id: EXP-NNN -->` and
`<!-- type: exploration -->`. Status: `exploration | rejected | graduated`.

When graduated, the exploration stays for history; normative rules move to
specs only.
