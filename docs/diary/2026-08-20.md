# 2026-08-20

**Change class: Standard (spec only).** UC-079 + session sampling — probabilistic
queue composition (T-W22). Owner GO 2026-08-20: no hard caps; weights from FSRS
`R`, sigmoid on held, `exp(−λN_new)`; learner-reaction scenarios in UC-079 and
supplement.

## Specs

- `docs/specs/service/session-sampling.md` (+ supplement, AC child)
- Updates: `session-builder`, `form-recall-pool`, `words-home`, `review-session`

## Use case

- `UC-079-build-a-core-vocabulary-with-natural-repetition.md` — nine reaction
  scenarios (R1–R9)

## Study

- Ch 43–44 already on disk; implementation map now points at session-sampling
  not foundation-phase hard caps

## Not done

- G1 `samplingReason` copy in review UI (optional follow-on)

## Shipped (same day)

- `lib/session-sampling.ts`, `lib/sampling-context.ts`
- Wired `buildSession` + `buildSessionAction`
- Soft form staging; base-building copy on `/words`

## T-W5 (Progress form breakdown)

**Change class: Standard.** Per-cell group breakdown on `/progress`; weak groups
link to `deck=form`.

- `lib/form-cell-groups.ts`, `lib/form-mastery-breakdown.ts`
- `features/progress/reading.ts`, `ProgressReport.tsx`
- Spec: `form-mastery-signal.md` rows 7–8

## T-W6 v1 (form practice foundation)

**Change class: Sensitive.** Inverse index + form-session mixing (rules 1–2).

- `lib/form-inverse-index.ts`, `lib/form-session-mixing.ts`
- Wired `mixFormSession` in `buildSession` when `deck=form`
- `lib/form-echo.ts` — echo rule in review session (pres↔pret twin)
- Spec: `form-inverse-index.md`; `form-practice.md` blockers cleared

## T-W11 (session loop line K4)

**Change class: Standard.** Session complete shows loop copy when meaning-recall
grades newly hold lemmas that shift persisted-source coverage.

- `lib/session-loop-line.ts` — coverage delta + link target
- `readSessionLoopLineAction`, `sessionLoopContext` on `buildSessionAction`
- `SessionComplete` + `useReviewSession` newly-held tracking
- i18n: `contentTrace.session.summary` / `session.link`
- AC: `content-traceability.acceptance-criteria.md` row 5

## T-W6 v3 (cell tasks + introduction + pull-forward)

**Change class: Sensitive.** Paradigm-cell task ids, runtime catalog, introduction
pacing (per-session cap + soft daily throttle), pull-forward when due sets block
mixing.

- `lib/form-cell-task-id.ts`, `lib/form-cell-catalog.ts`, `lib/form-introduction.ts`
- `lib/form-pull-forward.ts` — rule 4; wired in `session-builder` for `deck=form`
- `wordIdFromTaskId` handles cell ids; `buildSessionAction` builds cell pool
- **Owner correction 2026-08-20:** replaced hard 3/day cap with
  `min(5, sessionLength)` per session + `exp(−λN)` daily throttle (λ=0.15) +
  ceiling 15/day — aligned with [`study/44`](../study/44-foundation-phase-expert-review.md)
  and meaning-recall sampling
- Remainder: build/spoken answer routes; wrong-answer cell recording (spec blocker)

## T-W6 typed route (form answer input)

**Change class: Sensitive.** Typed production on form-recall cards with accent strip
and accent-forgiveness grading.

- `lib/form-answer-grade.ts`, `lib/form-accent-chars.ts`
- `features/review-session/FormAnswerInput.tsx`, `ReviewCard.tsx`
- Session cards carry `acceptedForms` + `lemmaSurfaces` from `buildSessionAction`
- Remainder: build route, spoken route
