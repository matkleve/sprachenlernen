# 2026-08-20

**Change class: Standard (spec only).** UC-079 + session sampling — probabilistic
queue composition (T-W22). Owner GO 2026-08-20: no hard caps; weights from FSRS
`R`, sigmoid on held, `exp(−λN_new)`; learner-reaction scenarios in UC-079 and
supplement.

## Specs

- `docs/specs/service/session-sampling.md` (+ supplement, AC child)
- Updates: `session-builder`, `form-recall-pool`, `words-home`, `review-session`

## Use case

- `UC-079-build-a-core-vocabulary-with-natural-repetition.md` — nine reaction
  scenarios (R1–R9)

## Study

- Ch 43–44 already on disk; implementation map now points at session-sampling
  not foundation-phase hard caps

## T-E12b + T-MV1 (shipped 2026-08-20)

- Footer segments: `activeStepIndex` + status → primary active bar (`bg-accent`)
- Prep rows: `border-x border-line-strong` for visible horizontal inset
- Step fixes: `Field` / `StepRenderProps` imports in runner steps
- T-MV1: `check-session-viability.mjs` in full `verify`; allowlist documents
  known gate failures per hosted method until T-MV2/T-MV5

## T-MV2 + T-MV6 (shipped 2026-08-20)

- **build-a-sentence:** budget-scaled batch (3–5 words), no prepare, reveal-answer
  with `honestyKey`; clears draft between loops
- **T-MV6:** menu `?minutes=` flows through srs-session card Start URLs — **superseded**
  by T-MV7 filter-only

## T-MV3 + T-MV5 (shipped 2026-08-20)

- **Session contract** on method detail above Start — `~N min · volume · feedback`
- `lib/method-session-contract.ts`; i18n keys under `methodMenu.sessionContract`
- **T-MV5:** `check-catalogue-budget.mjs` in `verify`; dictation budget unit escalation;
  free-production wall estimate includes runner chrome steps
- **T-MV5 follow-up (merged from main):** read-window budget scaling; srs-session
  card count from `minutes`; catalogue `3 min` removed from build-a-sentence;
  dictation fixture expanded — **superseded** by owner filter-only (T-MV7/T-MV8)

## Duration filter + content pipeline (specs 2026-08-20)

- **Owner decisions:** menu time = **filter only**; fixed packages on detail (≤ 2);
  `srs-session` always **15 cards**; reading = **full article**; catalogue news =
  **level adaptation** primary (study/45, study/46, study/48).
- **New specs:** `content-ingestion`, `content-adaptation`; contract stubs in
  `lib/content-ingestion.ts`, `lib/content-adaptation.ts`.
- **Use cases updated:** UC-007, UC-030, UC-029, UC-027, UC-039.
- **Queue:** IMPLEMENTATION-PLAN T-CI1–T-CI8; open gaps 24–30.

## T-MV7 + T-MV8 (shipped 2026-08-20, PR #164)

- Menu `?minutes=` = filter only (`min(durations) ≤ filter`); detail variant chips
  show **all** catalogue packages; Start uses selected package only
- SRS fixed 15 cards; no `minutes` on Start URL
- Material setup: `~N min` read estimate before Start; catalogue durations collapsed

## T-B11h chrome i18n stragglers (2026-08-20)

- Spec: `chrome-i18n-stragglers` — card report popover, demonstration sentence,
  content reading gloss, weekly reflection still on English `content.ts`
- Wired all four to `messages/{en,de}.json`; `buildWeeklyReflection` now takes
  localized copy from the server reader

## Words mixed-deck UX (2026-08-20, owner GO)

**Change class: Standard.** Owner rejected lemma/form deck pickers on `/words`
(icon-only forms, separate Forms card, Type/Build/Say tabs on review cards).

- **Words:** one **Start review** → `deck=mixed` (no `deck` query); paradigm-cell
  callout when inflection applies; same flip + 4 grades for meaning and form cards.
- **Form-only:** `deck=form` from Progress weak-pattern links and Methods — not
  from Words.
- Specs: `words-home.md`, UC-078, UC-063, UC-041, `docs/plans/words.md` T-W20,
  IMPLEMENTATION-PLAN pivot note.
- Code: `WordsHome.tsx` — removed `WordsReviewStartRow` / forms icon.

## Security audit — ten findings, all fixed (2026-08-20)

**Change class: Sensitive** (auth, deletion, persisted data). Red-test-first on
every finding with a code path; full `verify` green, justified by
VERIFY-SCOPES.md ("auth, DB, or persisted data paths touched").

Two were exploitable, and the rest were the conditions that let those two sit
unnoticed.

1. **`deleteAccount` trusted the session cookie.** Fixed with `getVerifiedAccount()`.
2. **Open redirect** in `/auth/callback` — Fixed with `safeInternalPath`.
3. **CI handed the service-role key to every `pull_request` run** — Split into
   environment-gated job.
4. **BACKEND.md §8 gates were prose.** `scripts/check-secrets.mjs` now in default scope.
5. **No security headers at all.** ADR-0013.
6. `create-test-user.ts` → `scripts/`, env-supplied, refuses production.
7. `task_state` update filtered on `task_id` only.
8. `content_sources.body` ceiling added (100 000).
9. "Complete archive" expanded per UC-024.
10. `/login?error=` — codes resolved from `messages/*.json`.

New traps recorded: `getSession()` verifies nothing; `startsWith("/")` is not a
path check.

## Study — learner intent onboarding (2026-08-20, evening)

Owner asked for Business / Alltag / Technik + skill intent at signup, with early
word boost so learners feel heard.

**Deliverable:** [`study/49-learner-intent-onboarding.md`](../study/49-learner-intent-onboarding.md)
— UX + LT + DS panel. **Consensus:** ask **after session 1** (UC-011), two taps,
skippable; 3–5 boosted lemmas per session at 1.25× max; G1 reason + profile edit;
extends UC-019 / F14 / study 24.

**Correction (same evening):** Owner rejected post-session-1 timing and skill
question. [`study/50-onboarding-popover-timing-and-skill-question.md`](../study/50-onboarding-popover-timing-and-skill-question.md)
— web research; **popover 2–3 screens after language pick**, **domain only**,
session 1 already shows boosted words; skill fork dropped (integrated skills + study 24 default).

**Register path (same evening):** Owner rejected „ein paar mischen“. [`study/51`](../study/51-register-path-and-interest-topics.md) — register curriculum + interests. **Then rejected 15/15 Business** — [`study/52`](../study/52-register-mix-ratio-calibration.md): **2–3 register cards per session**, rest basics, register-shaped sentences, **no 60-day decay**.

**Lernwelt collapse (same evening, late):** Owner rejected register+topic and hidden situations. [`study/56`](../study/56-lernwelt-single-choice.md) — **one flat Lernwelt** (Business, Alltag, Technik, Politik, Natur, Allgemein); weight in session composer only, not FSRS; no nagging copy.

## Specs — Lernwelt (2026-08-20, late)

- **New:** [`learner-world.md`](../specs/service/learner-world.md), [`learner-world-setup.md`](../specs/feature/learner-world-setup.md)
- **Updated:** `session-sampling` (+ supplement), `card-example-sentence`, `content-traceability`, `method-material-setup`, `profile`
- **Use cases:** UC-019 (full rewrite + spec links), UC-011 (optional popover)
- **Plan:** T-W23–T-W26 in [`plans/words.md`](../plans/words.md) + IMPLEMENTATION-PLAN Track B

## Owner decisions (2026-08-20, afternoon)

- **Ingest:** Wikinews first; lane C only when nothing licence-cleared; DW/BBC
  partner feeds wanted after legal review.
- **Adaptation level:** app-inferred skill tier — no manual A2 chip v1.
- **SRS:** always 15 cards; pad with new; no easy-vocab heuristic v1.
- **Review / fact-check:** no human politics queue; lane C = label only.
- **Progress:** adapted reading counts toward **reading skill** — label stays honest.
- **Upload / source-bound:** adapt → show **~N min** on detail → then Start enabled.
- **Detail chips:** show **all** catalogue packages; menu filter does not hide them;
  default = longest package (decision #36).
- **Band A2 + personal gate (#37):** shared A2 text may be **offered**; Start only
  when **personal** coverage on shown body passes (≥95 % / 80–94 % T1 / &lt;80 %
  block). Specs synced evening 2026-08-20.

## T-CI2 · Wikinews ingest (lane B v1)

- **`lib/wikinews-ingest.ts`** — allowlisted es/en origins, MediaWiki search/parse,
  wikitext→plain, `normalizeWikinewsPageToSource` with CC BY licence metadata.
- **`scripts/ingest-wikinews.mjs`** — CLI fetch; `npm run ingest:wikinews`.
- **`data/content/es.json`** — replaced `es-catalogue-chile` fixture with live
  `wikinews-es-3516` (Spanish Wikinews, CC BY).
- Default extensive-reading source id → `wikinews-es-3516`.

## T-CI3 · T2 adaptation + cache

- **`lib/catalogue-adaptation.ts`** — T0 skip when already comfortable; T2 rewrite +
  coverage validator (retry once); mandatory cache hit on second call.
- **`lib/adaptation-cache.ts`** — memory + file cache under `data/adaptations/`.
- **`lib/target-level.ts`** — held-pool proxy → A2/B1 until T-B3 CEFR mapping.
- **`scripts/adapt-catalogue.mjs`** — nightly batch; `npm run adapt:catalogue`.
- Production LLM rewrite still injectable — offline batch uses `ADAPTATION_REWRITE=fixture`.

## Shipped (Lernwelt branch)

- **T-W22** — session sampling module
- **T-W23** — service, onboarding, profile editor, worldMatch in sampling
- **T-W25** — example sentence bank + picker + review card display
- **T-W26** — `Source.world`, App-pick prefilter, `activeWorld` in material setup
- Lemma `worlds[]` curated tags in `data/starter/world-tags/`
- Sentence realizer dev page under Profile → Dev

## Not done

- **No fresh-context adversarial review.** AGENTS.md boundary 9 forbids
  subagents, and Sensitive changes require a reviewer that is not the
  implementer. This change has not had one — the gap is real, not waived.
- **No live browser check.** The CSP and the auth redirects are proven by unit
  tests and a green build only. See LIVE CHECK in the handoff.
- `content_sources` still has no delete path (⚠ SPEC GAP in `account-data.md`).
- **T-CI1** `Source.licence` on model + `loadSources` rejects catalogue rows without `licence.kind` — **shipped** (PR #167)
- **T-CI3+** adaptation labelling (T-CI4+)
- Code (`lib/session-sampling.ts`), messages for base-building copy, G1 reasons


## Copy voice — plain German, `du` everywhere (shipped 2026-08-20, evening)

**Change class: Standard.** Copy-only, but sweeping and it moves glossary terms,
so it was taken up a class rather than run as Trivial.

**User correction, in their words:** *"mir wirken sie irgendwie zu hochgestochen.
Weil irgendwie soll die App ja alle Menschen ansprechen aber die Sprache ist sehr
hoch."* Four causes were found, in order of effect:

1. **The address was split.** 168 `Sie` strings vs. 32 `du` — and the split ran
   right across the sign-up boundary: the landing page said *"Du suchst aus"*,
   the app two clicks later said *"Alles zu Ihrem Wortschatz"*. `profile` alone
   contained both. No single change introduced this; it drifted, because nothing
   owned it.
2. **Research vocabulary reached the surface.** Lemma, Paradigmenzelle,
   Paradigmata-Tableau (not a German word), Frequenzband, Signal, Niveaumodell,
   Lexikalische Abdeckung, Formbeherrschung, Erinnerungsstabilität.
3. **The register was essayistic.** *"der Nenner, den niemand in dieser Kategorie
   veröffentlicht"*, *"ist Arithmetik, kein Versagen"*, *"Echte Wahl ohne
   Entscheidungslast"*. 19 sentences ran past 22 words.
4. **Machine-translation damage**, which read as stiffness rather than as bugs:
   *"Wie viele Starter-Lemmas Sie an die Bedeutung erinnern"* (ungrammatical),
   *"mindestens zwei Abstände in der Wiederholung"* ("two spaced successes"),
   `Abstands-Wiederholungssitzung`.

### Owner decisions

| Question | Decision | Rules out |
| --- | --- | --- |
| Address | `du`, lowercase, everywhere | mixed surfaces; "formal for account pages" |
| Depth | core surfaces now; catalogue vocabulary later | a single sweep over all ~60 method entries |
| Technical terms | everyday word on the surface, term inside the disclosure | dropping the terms (loses checkability) and leaving them exposed |
| Locales | English moved with German | letting the source locale drift from the one people read |

### Promoted, not left in this entry

- **New:** [`COPY-VOICE.md`](../COPY-VOICE.md) — owns address, surface vocabulary,
  sentence length, and where copy lives. Linked from `AGENTS.md` § Read next
  (folded into the existing glossary row; the file sits on its 150-line cap).
- **New gate:** `scripts/check-i18n-address.mjs`, wired into `verify` as
  `i18n-address`. Checks `messages/de.json` **and**
  `data/i18n/method-catalogue/de.json` — a `Sie` reintroduced in the catalogue
  source would otherwise pass and then be synced into `messages/`.
- **`GLOSSARY.md`:** the "UI term = code term" rule now admits declared **surface
  words**, the mechanism **Card**/**Task** already used implicitly. Held → *Sitzt*,
  Fragile → *Wackelt*, Mature → *Sitzt fest*, Signal → *Messwert*, paradigm cell →
  *Wortform*, frequency rank → *Platz*.

### What was nearly missed

- `methodMenu.entries.*` is **generated**. Editing `messages/de.json` directly
  would have been reverted by the next
  `node scripts/sync-method-catalogue-i18n.mjs`. The 65 catalogue strings were
  changed in `data/i18n/method-catalogue/de.json` and synced.
- `en.reviewSession.loadError` is asserted equal to `sessionBuildFailed`'s
  `userMessage` in `lib/errors.ts`. Rewording the message alone broke
  `actions.test.ts`; the value was put back rather than editing `lib/errors.ts`.
- Three tests hard-coded copy instead of reading it from `en`/`de`. They now read
  the message, so the next copy change does not fail them:
  `shell-page-title`, `exercise-runner` (German checklist), `step-registry`.

### Bugs found while reading, and fixed

- `exerciseRunner.prepareItemTargetLang` said *"nicht auf Englisch"* in the German
  UI — a literal translation of "not in English" that names the wrong language for
  a German speaker. Now *"nicht auf Deutsch"*.

## Open threads (copy)

- **`lib/errors.ts` `userMessage`s are English-only** and are rendered to German
  users. Localising them means either i18n keys inside `lib/` (which AGENTS.md
  keeps framework-free) or a code→key map at the boundary. Not attempted here.
- **`/languages` is a public page in English.**
  `features/language-status/content.ts` never moved into `messages/`, and the
  German landing page links to it. Same for
  `features/progress/weekly-reflection/content.ts`.
- ~~Catalogue vocabulary~~ — **closed the same session**, see below.
- **`LIVE CHECK` not done** — see the handoff; no browser was driven this session.

## Copy voice, part 2 — the method catalogue (shipped 2026-08-20, evening)

**Change class: Standard.** Owner: *"gehe den Methodenkatalog an"*, and
`lib/errors.ts` staying English was accepted as-is — so that open thread is a
decision now, not a gap.

Rule 2 applied to all 59 entries, in `data/methods/*.json` (English, canonical)
and `data/i18n/method-catalogue/de.json`, then synced. `Prosodie`, `Chunks`,
`Kollokationskarten`, `Orthografie`, `Segmentierung`, `Kodierung`, `Lautform`,
`Automatisierung der Flexion`, `Verarbeitungsgeschwindigkeit`,
`Produktionsabruf`, `Wahrnehmung von Kontrasten`, `Abdeckung`, `Domäne`,
`Paraphrase`, `Signalquelle`, `Zeichenkette` are gone from the surface. A grep
for the list returns zero.

### The correction that mattered

The first pass deleted the technical names outright — `Shadowing` became
*"Mitsprechen, kurz versetzt"* and nothing else. That is worse, not plainer: a
learner who reads about shadowing anywhere else can no longer find it in the
catalogue. Four entries got the established name back, in parentheses after the
description:

| id | de |
| --- | --- |
| `shadowing` | Mitsprechen, kurz versetzt (Shadowing) |
| `dictogloss` | Hören, dann aus dem Kopf nachbauen (Dictogloss) |
| `minimal-pairs` | Ähnliche Laute unterscheiden (Minimalpaare) |
| `collocation-cards` | Wortpaare statt Einzelwörter (Kollokationen) |

`HVPT` was dropped rather than parenthesised — an acronym nobody searches for
buys nothing. `COPY-VOICE.md` § 2 now states the rule as *describe first, then
label*, which is what the code does; the earlier wording said the term belongs
"in the detail prose", which no field actually provides.

### Names that changed, and what depended on them

`srs-session` is now *Reviewing at growing intervals* / *Wiederholen mit
wachsenden Abständen*. `en.reviewSession.srsSessionName` duplicates the
catalogue name for the `/words/review` fast path, so both were moved together.

Five tests hard-coded English method names and would have failed silently on the
next rename; `practice.test.tsx` and `localize-method-entry.test.ts` now read
`en.methodMenu.entries[id].name`. Fixture names in `MethodCardAssetLab`,
`method-badge`, `method-skill-badges` and `review-session` were refreshed too —
they are literals that do not break, but a name that exists nowhere in the
catalogue is a trap for the next grep.

`docs/METHOD-IMPLEMENTATION-MATRIX.md` is generated and keys on ids, not names —
regenerating it produced no diff.
