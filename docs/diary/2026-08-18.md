# 2026-08-18

## Landing page study + mobile header

### Study / copy

- [`../study/38-landing-page-update.md`](../study/38-landing-page-update.md) —
  exploration for updating `/`: structure, headline I, phasing, owner decisions.
- Headline I shipped: method choice + checkable progress; thesis 1 in pillars.

### Mobile marketing header (merged from main)

Owner asked for the public header on phone-width viewports to match the signed-in
app shell: logo ghost chip left, centred brand title, hamburger menu right. Auth
controls moved into the menu popover; desktop keeps the inline row.

- Spec: `docs/specs/page/landing.md` — mobile layout section
- Implementation: `PublicHeader.tsx`, `PublicHeaderAuthControls.tsx`,
  `PublicHeaderMenu.tsx`

### Landing method preview (this session)

Owner: basic filters (time, skill, energy) + three catalogue methods; tap opens
explain-only dialog — no start.

### Demonstration sentence — flip card

Replaced word-marking with flip card + Hard / Good / Easy (no FSRS write).

### Reading surface v1 (T-W10 partial)

Tap-to-gloss text on `/content/[id]` — `ReadableText`, `lib/readable-text.ts`.


`ShellHeaderBar` — shared fixed/sticky chrome + scroll scrim for marketing and
app. `PublicHeader`, `DesktopShellHeader`, and mobile `FloatingShellChrome` all
use it. Marketing `main` gets mobile top padding like the app shell.

### Route-transition width (T-SHELL-05)

Owner reported horizontal snap when switching Methods ↔ Words. Root cause:
`app/(app)/loading.tsx` used `max-w-4xl` + mobile `pt-page-top` instead of
`ShellPageContent`; desktop scrollbar appeared/disappeared without gutter reserve.

- Spec: [`../specs/feature/page-layout.transitions.md`](../specs/feature/page-layout.transitions.md)
- `ShellPageLoading` + `scrollbar-gutter: stable` on `html` at `≥ md`

## Decisions

- **Landing ≠ Home** — study/24 demonstration sentence stays on signed-in Home.
- **Owner 2026-08-18:** headline I; screenshots/sandbox path; browser locale;
  signed-in stays on `/`.
- **Method preview:** reuse `MethodFilter` without refine; `pickDailyThree`;
  `Dialog` for explanation — no session start.

## Open

- Screenshot band (study/38 phase 1) after preview ships.
- Interactive try-box (phase 1b) — SRS default.
