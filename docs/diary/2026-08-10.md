# 2026-08-10 — Method menu UX specs: stepped time + instant filters

Owner screenshot: linear time slider and filter chips that reload the page.
Production is still on pre-PR #14/#15 behaviour; specs now state the contract
both PRs were written for.

| Area | What |
| --- | --- |
| Spec | `method-menu.md` + AC — stepped scale, `replaceState`, no scroll jump |
| Spec | `service/time-scale.md` — discrete steps 2 min → 1 day → Endless |
| UC | UC-045, UC-048 — instant filters + stepped time in success criteria |
| Docs | `ARCHITECTURE.md` client-first; `AGENT-PITFALLS.md` §17 |
| Component | `chip.md` — filters are FilterPill buttons, not Chip+Link |

**Merged to `main`:** `method-menu-ux-specs-d240`, `time-scale-slider-d240`,
`instant-menu-filters-d240` (combined stepped time + client-side filters).

**Review UX + routing (branch):** time label fixed height; flip-then-grade cards;
only `srs-session` uses `/words/review` from the method menu.

**Instant grade sync (spec only, awaiting GO):** `review-write-queue.md` —
local-first queue, advance immediately, background flush. Coder+UX agreed: no
"Saving…" on grade buttons.

**Shipped on `main`:** instant write queue (IndexedDB + `review_id` idempotency),
optimistic review session FSM, Words home (held/shaky/new, horizon, atlas).

## Error boundaries (UC-065 expansion)

Research: layered boundaries (global → route → feature → loader), failure
taxonomy, correlation ids — industry default is no longer generic "Something
went wrong". Specs: `errors-boundaries.md`, `route-error-surface.md`.
Implemented: `boundaryErrorFromUnknown`, `RouteErrorSurface`, `app/error.tsx`
replaced banned copy with operation-named messages (e.g. review session).

## Error pipeline Groups 1–3 + mobile nav plan

| Area | What |
| --- | --- |
| Errors | `database/*`, `session/build-failed`, `review/flush-failed` factories + wiring |
| Boundaries | `app/(app)/{words,methods,progress}/error.tsx` — shell survives |
| Correlation | Middleware `x-request-id` on every response |
| Mobile nav | Draft spec + plan: hamburger drawer + lucide icons on `<md` |

Research note: 3-destination apps usually use bottom tab bars; hamburger chosen
for v1 per owner phone-testing workflow. See `plans/mobile-nav.md`.

## Mobile nav v2 — floating pill + corner chips

Owner chose floating bottom pill (~85% width, icon + micro-label per destination)
and floating top-corner chips (back on drill-in, sign-out always) over hamburger.
Spec: `mobile-nav-v2.md`. Retires `MobileNav.tsx` drawer.
