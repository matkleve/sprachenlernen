# 2026-08-19

## Method overview before exercise runner

Owner report: tapping a runnable exercise method (e.g. build-a-sentence) jumped
straight into `/practice` — no method overview with settings and Start.

**Invariant:** exercise-runner methods use [`method-detail.md`](../specs/page/method-detail.md)
as the pre-start surface; only `srs-session` skips it from the menu.

**Cause:** T-B10g routed `cardHrefForMethod` to `/practice` for all runnable
methods, contradicting [`method-engines.md`](../specs/service/method-engines.md)
and [`method-menu.acceptance-criteria.md`](../specs/page/method-menu.acceptance-criteria.md).

**Fix:** `lib/method-session.ts` — cards link to `/methods/{id}` except card engine;
updated UC-042, UC-045, UC-046, UC-049, method-menu/card/detail/practice specs,
IMPLEMENTATION-PLAN, [`plans/method-card-destination.md`](../plans/method-card-destination.md).

**Audit (same session):** synced stale docs — `method-engines` counts + routing AC,
`method-menu.acceptance-criteria` hosted chip, `method-card.supplement`,
`practice-model.test` engine count.

**Layout:** material setup + Start moved above `trains` prose on method overview
(study/37 wireframe); setup panel uses raised card surface.

**Runner progress:** segmented step bars in footer above Weiter; seen = light gray,
done = light accent; removed continuous bar under hero.

**Spec sync (same session):** `exercise-runner.acceptance-criteria`,
`exercise-runner.states`, `practice-surface`, UC-049, `study/41` — footer
progress wording aligned with code.

**Method usefulness audit:** owner report — `build-a-sentence` fails feedback +
volume gates; study/42 + `method-session-viability` spec (draft).
