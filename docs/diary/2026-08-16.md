# 2026-08-16

## Remove Methods mirror debug route (`v0.8.0`)

Owner asked to drop the temporary second Methods page (`/methods-mirror`).

**Removed:** route, nav pill, `MethodsMirrorBanner`, safari-bisect hub link.
Nav back to three destinations (ADR-0009). Study/29 + study/31 updated.

## Words home layout parity with Methods (T-W17)

Owner asked to copy Methods page card rhythm onto `/words`. **Shipped:**

- Canvas intent copy (like `/methods` intro)
- Review + vocabulary section cards use `methodSectionSurface` + header graphics
- Uppercase section labels (`WordsSectionLabel`)
- Stat tiles show label + number only; definitions in collapsed disclosure
- Frequency band cards use vocabulary section shell (no per-tile prose)

Reuse: `MethodCardHeader`, `methodSectionSurface` from method-menu.
Spec: [`words-home.md`](../specs/feature/words-home.md).
