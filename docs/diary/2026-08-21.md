# 2026-08-21

**Change class: Sensitive (stateful/FSM UI + new learner-facing judgement).**
build-a-sentence becomes one write-and-check step; the runner gains per-step
answers, one descriptor per component, and step-owned completion.

Owner decisions (2026-08-21, in-session):

| Question | Decision |
| --- | --- |
| What may green claim? | Honest "nothing found" — never "correct" |
| Learner cannot reach green? | Prüfen required once; **Weiter** always open after |
| Where does the check run? | Server Action; rules framework-free in `lib/` |
| How far does the refactor go? | Full descriptor registry, not just the method |

## The iPhone question

Asked whether iOS could do the grammar check for us. It cannot, and the reason
matters enough to record: **no web API exposes spellcheck results to
JavaScript**, and iOS checks against the device's *installed keyboard
languages*, not the page language. A learner with a German keyboard would get
their Spanish silently rewritten, and our checker would then flag errors the
phone introduced. `autocorrect="off"` and `spellCheck={false}` on the sentence
field follow from that.

## Specs

- New: `docs/specs/service/sentence-check.md` (+ AC child) — linked from UC-017,
  which had listed no specs until now
- Updated: `exercise-runner.md`, `exercise-runner.states.md`,
  `exercise-step-components.md`, `exercise-recipe-composer.methods.md`,
  `method-session-viability.md`

## What the checker may say

Four rules, all provable from the shipped lemma table: `spelling`, `agreement`,
`person`, `missing-target`. Word order, word choice, idiom and style are out of
reach and stay silent. The load-bearing rule is that a finding needs the clash
to hold under **every** reading the table allows — one innocent reading clears
the token. `lib/sentence-check/corpus.test.ts` guards the quiet half: twenty
correct sentences that must produce no findings.

Evidence: Lyster & Ranta via [`STUDY-006`](../study/STUDY-006-production.md) —
prompts beat recasts, and `reveal-answer` was a recast. The same chapter is why
green cannot claim correctness.

## Two data facts that cost a debug round each

- **Nouns carry gender on the lemma, not the cell.** `forms.casa` is
  `[["casa","noun","sg"]]`; the `f` lives in `lemmas.casa`. Reading gender off
  the cell made every noun genderless, and a genderless noun agrees with
  everything — the whole agreement rule was silently dead. Adjectives are the
  opposite: their cell has the gender, and their lemma entry describes the noun
  sense of the same string (`blanco` → `m`), which fights every feminine form.
- **The reverse index needs a tie-break.** `el` at `f.sg` is `la`, but a
  treebank also annotates the stray token `a` as a form of `el`; at `m.pl` both
  `los` and an imported `els` sit at rank 0. Ranking by how central the reading
  is to each form fixes the first; the second has no principled winner in this
  data, so no suggestion is offered at all.

## Runner changes (each fixes something already wrong)

- **Per-step answers.** One session-wide `submitDraft` had to be cleared between
  repeated items, so the generic reducer carried a line naming
  `"type-with-word"` — and navigating back to item 1 showed an empty field,
  because its sentence had been cleared on the way past. Red test in
  `runner-state.test.ts`.
- **One descriptor per component.** The id lived in four hand-kept places with
  nothing checking they agreed; a component missing from the render switch fell
  through to *not built yet* copy in front of the learner with every test green.
  The renderer map is now a total record over descriptor ids.
- **Step-owned completion.** `canCompleteStep` knew `submit`/`accept` inline and
  the primary label switched on step type only, so no component could gate or
  rename its own button.

## Change-completeness

`type-with-word` is gone: component, i18n keys, registry entry, spec row, and
its references in `viability.ts` / `budget.ts` (replaced by `sentence-check` in
the production and learning-unit sets). `reveal-answer` stays — `cloze-sentences`
still wants it.

## Known red, not mine

`content-traceability` and the two `extensive-reading` catalogue-budget cases
fail on `main` at 839df32 and are untouched here.

## Open

- **⚠ SPEC GAP:** findings are not stored, so UC-017's "which kind of mistake is
  fading" trend has no data behind it yet. Deliberate — where a finding is
  persisted is a Sensitive question of its own.
- Suggestions could be ranked by `data/frequency/<code>.txt` instead of declining
  on a tie. Better UX, but it widens what the checker claims; not taken here.
