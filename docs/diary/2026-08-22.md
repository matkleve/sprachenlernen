# 2026-08-22

**Change class: Standard** — FFT wood synthesis breakthrough, assets landed, recipe retuned.

## Breakthrough — photographic wood FFT at native crack scale

Owner reaction on the sharp wood-01 remake: *"Omg this is so close"* — treated as
a **major breakthrough** for progression Workshop materials.

**Root cause of bubbly cracks (diagnosed this session):** `FEATURE_SCALE=9.0`
in `wood-grain-fourier-synthesis.py` blew the crack spectrum ~9× wider in pixel
space. Heeger–Bergen + `SPEC_SMOOTH_SIGMA` already soften edges; at scale 9 the
result is fat dark blobs with puffy rim halos, not fine checking lines. Profile
slice on wood-01: real cracks = narrow spikes; scale 9 = broad humps.

**Fix shipped:** `FEATURE_SCALE` default **9.0 → 1.0** (native size measured
from the source photo). Reference tile saved as
`design/progression/breakthrough-wood-01-final.png`.

**Assets landed (from `cursor/wood-patch-pipeline-948e`):**

- `design/progression/source-grid-wood-upload.png` — owner 3×2 grid
- `design/progression/patches/wood-01.png` … `wood-06.png` — one species each
- `design/progression/synthesized/` — recipe outputs per patch
- `scripts/slice-wood-texture-grid.py`, `scripts/fft-crack-layer-analysis.py`,
  `scripts/wood-synthesis-comparison.py`

**Still not done:** knots (wood-05/06) fail structurally; board validation via
`texture-metrics.mjs` not run yet; OKLCH colour ramp vs albedo-multiply open.

## Path towards awesome texture (owner GO)

| Step | What | Gate |
| --- | --- | --- |
| **1. Native-scale FFT tiles** | ✅ `FEATURE_SCALE=1`, six patches synthesized | Owner eye — wood-01 passed |
| **2. Measure against board** | `texture-metrics.mjs` on Workshop col 1 vs `breakthrough-wood-01-final.png` | Numbers + owner sign-off |
| **3. Wire dev preview** | T-PT0d: `public/design/progression/workshop-1.webp` from best tile | `/dev/progression` col 1 match |
| **4. Per-species tuning** | Optional `FEATURE_SCALE` / `CRACK_STRENGTH` per patch (03 contrast, 04 brightness) | Metrics diff, not one global knob |
| **5. Crack sharpness pass** | Lower `SPEC_SMOOTH_SIGMA`, optional unblurred rim on crack mask | A/B vs breakthrough reference |
| **6. Knot mechanism** | Domain warp + radial stamp (STUDY-032 open) — only if board needs knots | Spec first |
| **7. Colour exactness** | STUDY-031 OKLCH quantile ramp instead of albedo×shading | hue/chroma within STUDY-031 bands |
| **8. Production** | Promote tile to shipped skin only after phase 0 board sign-off | `progression-reference-board.md` |

**Trust order unchanged:** owner → spec → code → board → study.

## PR

Merged **#195** (`cursor/fft-crack-layer-analysis-f642`) — patches, synthesis
retune, tooling, breakthrough reference.
