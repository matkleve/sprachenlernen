# 2026-08-22 — Wood crack synthesis + metrics

## Shipped

- Morphological valley extraction (`--crack-source morphological`) replacing stripe procedural
- `scripts/wood-crack-metrics.py` — crack-mask structure QA (components, blob px, width, corr)
- `scripts/build-morph-comparison.py` — visual montage
- `docs/study/STUDY-033-wood-crack-morphology.md` — mathematician + naturalist account

## Decisions

- **Hero cracks = extracted, not generated** — photo `relu(blur−photo)` mask layered directly.
  Rules out: HB and morph as default for production tiles.
- **Morphological for placement only** — unpredictable hills/valleys, but metrics show blobs not fissures (v4 ~8 components vs extracted ~1500).
- **Two metric scripts** — `texture-metrics.mjs` on finals; `wood-crack-metrics.py` on crack masks.
  Rules out: eyeball-only or finals-only verification.

## Corrections

- User: "4 stripes interrupted but wood is less predictable" → stripe procedural rejected;
  morphological valley extract on synthetic topography.
- User: "use morphological valley and hills extraction" → not regular dashes, not HB blobs.
- User: metrics should be in repo to verify → added `wood-crack-metrics.py`; STUDY-033 documents both lenses.

## Went wrong

- Morph v1: fine grain in height field → thousands of micro-valleys (salt-and-pepper).
  Fix: coarse hills only, extract on smoothed topography.
- Morph v3/v4: `grey_closing` merges valleys into ~2200 px blobs. Metrics confirm; naturalist reads as ponds not checking.
  Next: watershed/skeleton v5, drop wide closing.

## Open

- Morph v5 (centerline extraction) to hit extracted component stats
- Run metric suites on real 2×3 grid once owner commits `source-grid-wood-upload.png`
- STUDY-032 gate: `texture-metrics` vs `reference-board.png`
