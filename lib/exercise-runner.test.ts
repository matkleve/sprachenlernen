/**
 * Contract: docs/specs/feature/exercise-runner.md
 * Contract: docs/specs/page/practice.md
 * Red-test scaffold — implement T-E1+.
 */
import { describe, it } from "vitest";

describe("exercise-runner", () => {
  it.todo("marks seen on navigation without marking done");
  it.todo("keeps wait timer running across step navigation");
  it.todo("requires photo or text before submit done");
  it.todo("ends with no backlog when decide is declined");
});
