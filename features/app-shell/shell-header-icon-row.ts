/**
 * Horizontal row of header icon chips (brand, language, destinations, account).
 * gap-3 — same rule as button.md / landing.md: expanded sm hit targets must not overlap.
 */
export const shellHeaderIconRowClass = "flex min-w-0 items-center gap-3";
