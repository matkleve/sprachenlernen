/** Decorative header for the Words home review card — mirrors method section graphics. */
export const wordsReviewGraphicSrc = "/assets/words/words-home-review.webp";

export function wordsReviewGraphicAlt(label: string): string {
  return `${label} — decorative header`;
}
