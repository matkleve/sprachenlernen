# Grain creator reference crops

Drop cropped reference-board images here for side-by-side comparison on
`/dev/grain-creator`.

| File | Source |
| --- | --- |
| `workshop-1-raw-planks.svg` | Workshop column 1 stand-in — procedural horizontal valleys until the photo crop lands |
| `workshop-1-raw-planks.webp` | Optional photo crop from the reference board (replace the SVG when available) |

Export as WebP, crop tight to the wood background only (no UI chrome).
