node_modules/
.next/
.next-verify/
out/
build/
coverage/

.env
.env*.local

*.tsbuildinfo
next-env.d.ts

.DS_Store
.vscode/
.idea/

# Throwaway measurement scripts (docs/TRAPS.md § measure, don't eyeball)
tmp/

# Cached morphology sources for scripts/build-lemma-tables.mjs — large,
# network-fetched, and reproducible. The generated data/lemma/*.json is
# committed; these are not.
.cache/

# Written by `supabase link`. Machine-local state, and pooler-url embeds the
# database user — the migrations under supabase/migrations/ are the artefact
# worth committing, not the link.
supabase/.temp/
