"use client";

import { DestinationError } from "@/features/app-shell/DestinationError";

/**
 * Signed-in route errors keep the shell (profile, language picker, etc.).
 * Contract: docs/specs/service/errors-boundaries.md
 */
export default DestinationError;
